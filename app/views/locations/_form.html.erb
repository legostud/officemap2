<%= form_for(@location) do |f| %>
  <% if @location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% @location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :location_num %><br />
    <%= f.text_field :location_num %>
  </div>
  <div class="field">
    <%= f.label :x_cord %><br />
    <%= f.number_field :x_cord %>
  </div>
  <div class="field">
    <%= f.label :y_cord %><br />
    <%= f.number_field :y_cord %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
